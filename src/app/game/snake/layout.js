export const metadata = {
  title: "Snake | Rick's Portfolio",
  description: "Classic grid snake in the browser—arrow keys, score, Solarized styling.",
};

export default function SnakeGameLayout({ children }) {
  return children;
}
