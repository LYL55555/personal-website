export const metadata = {
  title: "Pokémon Quiz | Rick's Portfolio",
  description:
    "Guess Pokémon from PokéAPI clues—multiple generations, trilingual names, and speed scoring.",
};

export default function PokemonQuizLayout({ children }) {
  return children;
}
