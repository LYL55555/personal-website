# Copy to `.env.local` and fill in your values (never commit secrets).

# Google reCAPTCHA (contact form)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=

# Add your email/API keys for `/api/email/send` or other routes here.
